const Influencer = require('../models/influencer');
const extError = require('../util/error/extError');
const fetch = require('node-fetch');

class InfluencerServices{

    async createInfluencers(){
        const influencers = fetch
    }
}